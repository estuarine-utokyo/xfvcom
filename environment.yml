name: xfvcom
channels:
  - conda-forge
dependencies:
  # Python runtime
  - python=3.13.*

  # Core scientific packages
  - numpy
  - pandas
  - xarray
  - scipy
  - scikit-learn

  # Visualization packages
  - matplotlib
  - cartopy
  - pyproj
  - imageio
  - imageio-ffmpeg
  - moviepy

  # Interactive visualization (optional but recommended)
  - hvplot
  - bokeh
  - holoviews

  # NetCDF support
  - netcdf4
  - h5netcdf
  - cftime

  # Additional required packages
  - tqdm
  - jinja2
  - pyyaml
  - chardet
  - pvlib  # Solar radiation estimation for gap filling

  # Development tools
  - pytest
  - mypy
  - black
  - isort
  - ruff
  - ipykernel
  - jupyterlab

  # Type stubs for mypy
  - types-pyyaml
  - pandas-stubs

  # Pip dependencies (install after conda)
  - pip
  - pip:
      # Install xfvcom package in editable mode
      - -e .
